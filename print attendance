#include <stdio.h>

int main() {
    int n, i;

    printf("Enter total number of students: ");
    scanf("%d", &n);

    printf("Attendance Numbers of Students:\n");
    for(i = 1; i <= n; i++) {
        printf("Student %d\n", i);
    }

    return 0;
}
