#include <stdio.h>
#include <string.h>

int main() {
    char password[50];

    printf("Enter password: ");
    scanf("%s", password);

    if (strlen(password) >= 6)
        printf("Password is valid\n");
    else
        printf("Password is invalid (minimum 6 characters required)\n");

    return 0;
}
