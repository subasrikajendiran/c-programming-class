#include <stdio.h>

int main() {
    int m1, m2, m3;
    float avg;

    printf("Enter three marks:\n ");
    scanf("%d %d %d", &m1, &m2, &m3);

    avg = (m1 + m2 + m3) / 3.0;

    printf("Average Marks = %.2f\n", avg);

    return 0;
}
