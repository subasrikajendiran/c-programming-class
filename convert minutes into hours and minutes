#include <stdio.h>

int main() {
    int minutes, hours, rem;

    printf("Enter total minutes: ");
    scanf("%d", &minutes);

    hours = minutes / 60;
    rem = minutes % 60;

    printf("%d minutes = %d hours %d minutes\n", minutes, hours, rem);

    return 0;
}
